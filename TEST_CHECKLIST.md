# 테스트 체크리스트

이 문서는 구현이 제대로 되었는지 확인하는 테스트 절차입니다.

## ✅ 1단계: 기본 빌드 테스트

### 빌드 성공 확인
```bash
npm run build
```
- ✅ 빌드가 성공적으로 완료되어야 합니다
- ✅ `dist` 폴더가 생성되어야 합니다

**현재 상태**: ✅ 완료

---

## ✅ 2단계: 개발 서버 실행 테스트

### 개발 서버 실행
```bash
npm run dev
```

### 브라우저에서 확인 (`http://localhost:3000`)

#### 기본 UI 확인
- [ ] 페이지가 로드됩니다
- [ ] 헤더에 "INSTANT BOARD"가 표시됩니다
- [ ] 하단에 "+" 버튼이 표시됩니다
- [ ] "Connecting to Cloud Board..." 또는 빈 보드가 표시됩니다

#### Supabase 미설정 시 예상 동작
- [ ] 브라우저 콘솔에 경고 메시지가 표시됩니다:
  ```
  Supabase credentials are missing. Please set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in your .env file.
  ```
- [ ] 동기화 상태가 "Offline" (빨간색)으로 표시됩니다
- [ ] 보드 생성이 실패하고 빈 보드가 표시됩니다

**이것은 정상입니다!** Supabase 설정이 필요합니다.

---

## ✅ 3단계: Supabase 설정 후 테스트

### 3.1 Supabase 프로젝트 생성
- [ ] Supabase 계정 생성 완료
- [ ] 새 프로젝트 생성 완료
- [ ] 프로젝트 URL과 anon key 복사 완료

### 3.2 데이터베이스 스키마 실행
- [ ] Supabase 대시보드 → SQL Editor 이동
- [ ] `supabase/schema.sql` 파일 내용 실행
- [ ] 성공 메시지 확인
- [ ] Table Editor에서 `boards` 테이블 확인

### 3.3 환경 변수 설정
- [ ] 프로젝트 루트에 `.env` 파일 생성
- [ ] 다음 변수 설정:
  ```env
  VITE_SUPABASE_URL=your_supabase_url
  VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
  ```

### 3.4 개발 서버 재시작
```bash
# Ctrl+C로 서버 중지 후
npm run dev
```

### 3.5 기능 테스트

#### 보드 생성 테스트
- [ ] 페이지 로드 시 자동으로 새 보드가 생성됩니다
- [ ] URL에 `?b=보드ID`가 추가됩니다
- [ ] 동기화 상태가 "Cloud Synced" (초록색)로 표시됩니다

#### 노트 생성 테스트
- [ ] "+" 버튼 클릭
- [ ] 모달이 열립니다
- [ ] 제목 입력
- [ ] 내용 입력
- [ ] 비밀번호 입력 (`1212`)
- [ ] "Post Note" 버튼 클릭
- [ ] 노트가 보드에 표시됩니다
- [ ] 동기화 상태가 "Synced"로 변경됩니다

#### 노트 상세 보기 테스트
- [ ] 노트 클릭
- [ ] 상세 모달이 열립니다
- [ ] 제목, 내용, 작성자, 날짜가 표시됩니다
- [ ] "닫기" 버튼으로 모달 닫기

#### 노트 삭제 테스트
- [ ] 노트에 마우스 오버
- [ ] 삭제 버튼 (X) 표시 확인
- [ ] 삭제 버튼 클릭
- [ ] 삭제 확인 모달이 열립니다
- [ ] 비밀번호 입력 (`1212`)
- [ ] "삭제하기" 버튼 클릭
- [ ] 노트가 보드에서 제거됩니다

#### 드래그 앤 드롭 테스트
- [ ] 노트를 마우스로 드래그
- [ ] 다른 위치로 이동
- [ ] 순서가 변경됩니다
- [ ] 동기화 상태가 "Syncing..."으로 변경 후 "Synced"로 변경

#### 공유 링크 테스트
- [ ] "Share" 버튼 클릭
- [ ] 링크가 클립보드에 복사됩니다
- [ ] 새 탭에서 링크 열기
- [ ] 동일한 보드가 표시됩니다
- [ ] 새 탭에서 노트 생성
- [ ] 원래 탭에서도 새 노트가 표시됩니다 (새로고침 필요할 수 있음)

#### AI 개선 기능 테스트 (선택사항)
- [ ] 노트 생성 모달에서 내용 입력
- [ ] "✨ Magic Polish" 버튼 클릭
- [ ] 내용이 개선됩니다 (Gemini API 설정 필요)

---

## ✅ 4단계: 빌드 및 배포 테스트

### 프로덕션 빌드
```bash
npm run build
```

### 빌드 결과 미리보기
```bash
npm run preview
```

### 확인 사항
- [ ] 빌드가 성공적으로 완료됩니다
- [ ] `dist` 폴더에 파일이 생성됩니다
- [ ] 미리보기 서버에서 정상 작동합니다

---

## 🐛 문제 해결

### 문제: "Supabase credentials are missing" 경고
**해결**: `.env` 파일을 생성하고 환경 변수를 설정하세요.

### 문제: "boards 테이블이 존재하지 않습니다" 오류
**해결**: `supabase/schema.sql` 파일을 Supabase SQL Editor에서 실행하세요.

### 문제: 보드 생성 실패
**해결**: 
1. Supabase 프로젝트가 활성화되어 있는지 확인
2. RLS 정책이 올바르게 설정되었는지 확인
3. 브라우저 콘솔의 에러 메시지 확인

### 문제: 노트가 저장되지 않음
**해결**:
1. 비밀번호가 `1212`인지 확인
2. 브라우저 콘솔의 에러 메시지 확인
3. Supabase Table Editor에서 데이터 확인

---

## 📝 테스트 결과 기록

테스트 날짜: _______________

- [ ] 1단계: 기본 빌드 테스트 완료
- [ ] 2단계: 개발 서버 실행 테스트 완료
- [ ] 3단계: Supabase 설정 후 테스트 완료
- [ ] 4단계: 빌드 및 배포 테스트 완료

발견된 문제:
_______________________________________
_______________________________________
_______________________________________

---

**다음 단계**: 모든 테스트가 통과하면 Vercel 배포를 진행하세요!

